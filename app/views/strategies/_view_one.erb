title: <%= strategy.title %>
<BR>
Insights: <%= strategy.description %>
<BR>
<% strategy.activities.each do |activity| %>
Activity:	<%= activity.id %> -- <%= link_to activity.title, mypd_path(activity.id) %>
<BR>
<% end %>