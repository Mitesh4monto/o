<% @title = "My Plan" %>
<% @show_aside = false %>

	 <!-- <%= raw myp_status %>  -->
	    	<% if !current_user.strategy.current_activities.empty? %>
			<%= 
			 render :partial => 'view_two', locals: {strategy: current_user.strategy, myp: true} 
			%>

			<% else %>
	        <div class="box_holder3">
              <div class="box marginnone">
                <p>Your plan is empty.  Go ahead and add activities to it.</p>
              </div>
            </div>
            <div class="clear"></div>
            <div class="box_3"><%= link_to 'Add activity', add_activity_path, :class => 'green' %>
			<%= link_to 'Look for courses', courses_path %>
              <div class="clear"></div>
            </div>
			<% end %>
