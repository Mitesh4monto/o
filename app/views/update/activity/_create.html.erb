create activity  
<% if update.loggable %>
	<% activity =  update.loggable %>
	<% if activity.course_activity? %>
		in course <%= link_to activity.strategy.course.name, activity.strategy.course %>
	<% else %>
		<%= link_to activity.title, activity.user %>'s 	
	<% end %>
<% else %>
 a <%= update.loggable_type %> which has since been removed 
<% end %>